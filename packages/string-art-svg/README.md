# String Art Svg

Consumes the generated vertices matrix from [@chthonic/string-art](https://www.npmjs.com/package/@chthonic/string-art) and converts it to an svg.

A cli tool is also available to generate the SVG.
